server:
  port: 8080

spring:
  config:
    activate:
      on-profile: local

  jpa:
    hibernate:
      ddl-auto: create

logging.level:
  org.hibernate.SQL: debug

logging-module:
  version: 0.0.0

